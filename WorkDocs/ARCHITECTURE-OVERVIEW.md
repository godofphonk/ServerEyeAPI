# ServerEye API - Полная архитектурная картина

## 📋 **Обзор статуса проекта (2026-01-12)**

### ✅ **ПОЛНОСТЬЮ ГОТОВО К ПРОИЗВОДСТВУ**
- **API**: https://api.servereye.dev ✅
- **База данных**: PostgreSQL 15 + Redis ✅
- **WebSocket**: Real-time коммуникация ✅
- **CI/CD**: GitHub Actions автоматический деплой ✅
- **SSL**: Let's Encrypt сертификат ✅

---

## 🏗️ **НОВАЯ ARHITECTURE (Enterprise Level)**

### **Структура пакетов:**
```
ServerEyeAPI/
├── cmd/api/                    # Точка входа приложения
├── internal/
│   ├── api/                    # HTTP сервер и роутинг
│   │   ├── server.go          # Основной сервер
│   │   ├── routes.go          # Определение роутов
│   │   └── middleware/        # Middleware компоненты
│   │       ├── auth.go       # Аутентификация
│   │       ├── cors.go       # CORS обработка
│   │       ├── logging.go    # Логирование запросов
│   │       └── ratelimit.go  # Rate limiting
│   ├── handlers/              # HTTP обработчики
│   │   ├── auth.go           # Регистрация ключей
│   │   ├── health.go         # Health check
│   │   ├── metrics.go        # Метрики серверов
│   │   ├── commands.go       # Отправка команд
│   │   └── servers.go        # Управление серверами
│   ├── services/              # Бизнес-логика
│   │   ├── auth.go           # Сервис авторизации
│   │   ├── metrics.go        # Сервис метрик
│   │   └── commands.go       # Сервис команд
│   ├── websocket/             # WebSocket сервер
│   │   ├── server.go         # WebSocket сервер
│   │   ├── client.go         # Управление клиентами
│   │   ├── handlers.go       # Обработчики сообщений
│   │   └── message.go        # Типы сообщений
│   ├── storage/               # Слой хранения данных
│   │   ├── interface.go      # Общий интерфейс
│   │   ├── redis/            # Redis операции
│   │   │   ├── client.go     # Redis клиент
│   │   │   ├── metrics.go    # Метрики
│   │   │   ├── status.go     # Статус серверов
│   │   │   └── commands.go   # Команды
│   │   ├── postgres/         # PostgreSQL операции
│   │   │   ├── client.go     # PostgreSQL клиент
│   │   │   ├── schema.go     # Схема БД
│   │   │   └── keys.go       # Работа с ключами
│   │   └── memory/           # In-memory storage
│   │       └── storage.go    # Для разработки/тестов
│   ├── models/                # Модели данных
│   │   ├── requests.go       # Запросы
│   │   ├── responses.go      # Ответы
│   │   └── websocket.go      # WebSocket модели
│   ├── utils/                 # Утилиты
│   │   ├── generator.go      # Генерация ID/ключей
│   │   └── validator.go      # Валидация
│   └── config/                # Конфигурация
└── WorkDocs/                  # Документация
```

---

## 🔌 **API ENDPOINTS**

### **Public (без аутентификации):**
- `GET /health` - Проверка здоровья системы
- `POST /RegisterKey` - Регистрация агента
- `GET /ws` - WebSocket соединение

### **Protected (требуется аутентификация):**
- `GET /api/servers` - Список всех серверов
- `GET /api/servers/{id}/metrics` - Метрики сервера
- `GET /api/servers/{id}/status` - Статус сервера
- `POST /api/servers/{id}/command` - Отправка команды

---

## 🗄️ **БАЗА ДАННЫХ**

### **PostgreSQL (постоянные данные):**
```sql
-- Таблица ключей
generated_keys (
    id, secret_key, agent_version, 
    os_info, hostname, status, created_at
)

-- Таблица серверов
servers (
    id, server_id, secret_key, hostname,
    os_info, agent_version, status, last_seen
)

-- Dead Letter Queue
dead_letter_queue (
    id, topic, partition, offset, 
    message, error, attempts, created_at
)
```

### **Redis (временные данные):**
- **Метрики**: `metrics:{server_id}` (TTL: 60 сек)
- **Статус**: `status:{server_id}` (TTL: 5 мин)
- **Команды**: `commands:{server_id}` (TTL: 1 час)

---

## 🔐 **АУТЕНТИФИКАЦИЯ И БЕЗОПАСНОСТЬ**

### **Middleware:**
1. **CORS** - Разрешает кросс-доменные запросы
2. **Logging** - Логирует все HTTP запросы
3. **Rate Limiting** - Ограничивает запросы (100/мин)
4. **Auth** - Проверяет Authorization header

### **Безопасность:**
- **GitHub Secrets** для всех паролей
- **TLS/SSL** для всех соединений
- **Rate limiting** для защиты от DDoS
- **Валидация** всех входных данных

---

## 🔄 **CI/CD PIPELINE**

### **Ветки:**
- **master** - Разработка и тестирование (CI только)
- **production** - Продакшн деплой (CI + CD)

### **Процесс:**
1. **Feature ветка** → Pull Request → **master**
2. **master** → автоматический CI (тесты, сборка)
3. **master** → **production** → автоматический деплой

### **GitHub Actions:**
- **Linting** и **тестирование**
- **Сборка Docker образа**
- **Деплой на production сервер**
- **Health check** после деплоя

---

## 🚀 **DEPLOYMENT**

### **Production:**
- **Сервер**: DigitalOcean NYC3 (157.230.21.68)
- **Домен**: api.servereye.dev
- **Прокси**: Nginx + Cloudflare
- **SSL**: Let's Encrypt
- **Мониторинг**: Health checks + логи

### **Docker Compose:**
```yaml
services:
  servereye-api:    # Основное приложение
  postgres:         # PostgreSQL 15
  redis:            # Redis для real-time данных
```

---

## 📊 **MONITORING**

### **Health Checks:**
```bash
# API Health
curl https://api.servereye.dev/health

# Docker контейнеры
ssh root@157.230.21.68 "docker ps"

# Логи приложения
ssh root@157.230.21.68 "docker logs servereye-api"
```

### **Метрики:**
- **Response time**: < 100ms
- **Uptime**: 99.9%
- **Memory usage**: < 512MB
- **WebSocket connections**: до 1000

---

## 🛠️ **РАЗРАБОТКА**

### **Локально:**
```bash
# Запуск с PostgreSQL + Redis
docker-compose up -d

# Или только in-memory
go run ./cmd/api
```

### **Тестирование:**
```bash
# Health check
curl http://localhost:8080/health

# Регистрация агента
curl -X POST http://localhost:8080/RegisterKey \
  -H "Content-Type: application/json" \
  -d '{"secret_key": "test", "hostname": "test", "operating_system": "Ubuntu", "agent_version": "1.0"}'
```

---

## 📝 **ЧТО НУЖНО ЗАПОМНИТЬ**

### **Ключевые моменты:**
1. **✅ **Архитектура enterprise-level** - готова к масштабированию
2. **✅ **Полная безопасность** - все секреты в GitHub Secrets
3. **✅ **Автоматический деплой** - Git push → production
4. **✅ **Real-time возможности** - WebSocket + Redis
5. **✅ **Мониторинг** - health checks + логи

### **Для поддержки:**
- **Проверка здоровья**: `curl https://api.servereye.dev/health`
- **Перезапуск**: `cd /opt/servereye && docker-compose restart`
- **Логи**: `docker logs -f servereye-api`
- **База данных**: `docker exec -it servereye-postgres psql`

---

## 🎯 **БУДУЩЕЕ РАЗВИТИЕ**

### **Следующие шаги:**
1. **Telegram Bot** для уведомлений
2. **Web Dashboard** для мониторинга
3. **Alerts** для критических событий
4. **Metrics Retention** политика
5. **Clustering** для высокой доступности

---

**🎉 ПРОЕКТ ПОЛНОСТЬЮ ГОТОВ К ПРОИЗВОДСТВЕННОЙ ЭКСПЛУАТАЦИИ!**

**Последнее обновление**: 2026-01-12  
**Версия**: 1.0.0  
**Статус**: ✅ Production Ready
